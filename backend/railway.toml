[build]
builder = "dockerfile"

[deploy]
# startCommand intentionally omitted — Dockerfile CMD handles this with
# shell form so Railway's $PORT env var is correctly expanded at runtime
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
